# speedtest
A simple internet speed test site made by me

# Usage

just download the zip file and run main.py. you need to have `flask`, `wtforms`, `flask_wtf`, `speedtest` and `ujson` installed from pip. you need to have c++ redistributable installed
